import {Text, View} from 'react-native';
import {StockList} from '../../components';

const Home = () => {
  return (
    <View style={{backgroundColor:"", flex:1}}>
      <StockList />
    </View>
  );
};

export default Home;
